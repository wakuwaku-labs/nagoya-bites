# 夜間QAシークレットスキャン（scripts/security_audit.js）の誤検知除外
# 書式: 「パス部分文字列」＝ファイルごと除外 / 「パス部分文字列::行の部分文字列」＝その行だけ除外
# 追加するときは、なぜ公開して問題ないかの検証根拠を必ずコメントに残す。

# 店舗詳細モーダル「電話する」ボタン用の Maps JavaScript API キー（PR #224）。
# ブラウザで読み込む仕様上クライアントに置くしかない公開キー。2026-09-14 に
# HTTPリファラ制限が効いていることを実測で確認済み: リファラ無しで Places Details
# Web Service を呼ぶと Google が "API keys with referer restrictions cannot be used
# with this API." で REQUEST_DENIED を返す／Geocoding は API 自体が無効。
# 鍵をローテーションした場合もこの行は変数名で当たるので更新不要。
index.html::var NB_CALL_MAPS_KEY = 'AIza
